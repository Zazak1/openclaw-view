/**
 * Minimal type for Control UI build. Full definition lives in OpenClaw main repo.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type AnyAgentTool = { name: string; [key: string]: any };
